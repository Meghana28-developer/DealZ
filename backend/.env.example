# DEALZ Backend Configuration Example

# Server configuration
PORT=8000
HOST=0.0.0.0

# SQLite Database Path
DATABASE_URL=sqlite:///./dealz.db

# Google Gemini API Key (Required for active agent mode, falls back to rule-based engine if empty)
GEMINI_API_KEY=your_gemini_api_key_here
